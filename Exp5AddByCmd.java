class Exp5AddByCmd {
    public static void main(String a[])
    {
        int  x=Integer.parseInt(a[0]);
        int  y=Integer.parseInt(a[1]);
        System.out.print("Sum of: "+x+" & "+y+" is: "+(x+y));
    }
}

